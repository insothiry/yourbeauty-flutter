// List of Enums
// They cannot be created inside a class

enum TextSizes { small, meduim, large }

enum OrderStatur { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  visa,
  googlePay,
  applePay,
  masterCard,
  creditCard,
  qrCode
}
